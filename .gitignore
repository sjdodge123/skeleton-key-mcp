node_modules/
dist/
*.log

# Runtime data — never commit secrets, vault cache, or the local store
data/
*.sqlite
*.sqlite3
bootstrap.store
.bw-cache/
config.local.yaml

# Env
.env
.env.local

# Homelab-specific local notes (private IPs/topology — never commit to a public repo)
local/

# OS
.DS_Store
